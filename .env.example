# Leave empty to use the bundled same-origin mock API routes.
NEXT_PUBLIC_API_BASE_URL=

# Use a long random value in deployed environments to sign JWT sessions.
JWT_SECRET=replace-with-a-long-random-secret

# Configure these in Stripe Dashboard; never commit real keys.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
